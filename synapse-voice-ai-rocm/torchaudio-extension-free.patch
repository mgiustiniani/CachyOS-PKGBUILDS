--- a/torchaudio/_extension/__init__.py
+++ b/torchaudio/_extension/__init__.py
@@ -34,7 +34,10 @@ if os.name == "nt" and (3, 8) <= sys.version_info < (3, 9):
 # In case of an error, we do not catch the failure as it suggests there is something
 # wrong with the installation.
-_IS_TORCHAUDIO_EXT_AVAILABLE = _load_lib("_torchaudio")
+# Synapse Voice AI uses only the pure-Python resampling and Kaldi feature
+# implementations. Do not load a version-specific TorchAudio C++ extension;
+# the system ROCm PyTorch package provides the tensor runtime.
+_IS_TORCHAUDIO_EXT_AVAILABLE = False
 _IS_ALIGN_AVAILABLE = False
 if _IS_TORCHAUDIO_EXT_AVAILABLE:
     if not _load_lib("libtorchaudio"):
